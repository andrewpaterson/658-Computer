$start_address 0x800

sub @count_pointless(int16 start, int16 _end, int8* address):int16
	flag p;
	int8 value;
	int16 i = start;
#loop:
	address[i] = value;
	i++;
	p:i - (_end + 3);
	if p.n || p.z #loop;
	i -= start
	return i;
end

sub @@main:
	int16 count = @count_pointless(0, 23, 9);
		
	int32 number; int32 another;
	number = 5;
	another = 6;
	number = (number + ((3 + 6) * another) / 2);
	number = (number + temp1);
end

